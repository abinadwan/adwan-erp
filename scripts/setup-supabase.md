# Supabase Setup

```bash
supabase init
supabase start
supabase db push
supabase db seed
```

Create buckets:

```bash
supabase storage create-bucket employees-avatars --public
supabase storage create-bucket employees-cv
```
